diff a/src/gui_app/services/config_service.py b/src/gui_app/services/config_service.py	(rejected hunks)
@@ -3,6 +3,7 @@ from __future__ import annotations
 import re
 from pathlib import Path
 
+from src.entry.portable_runtime import resolve_tool_root
 
 class ConfigService:
     CAMERA_CONFIG_RE = re.compile(r"^camera\.(?P<name>.+)\.json$", re.IGNORECASE)
@@ -10,7 +11,7 @@ class ConfigService:
 
     def __init__(self, project_root: Path):
         self.project_root = project_root.resolve()
-        self.calibration_root = self.project_root / "Data" / "Script" / "CameraCalibration"
+        self.calibration_root = resolve_tool_root()
         self.config_dir = self.calibration_root / "configs"
 
     def list_cameras(self) -> list[str]:
